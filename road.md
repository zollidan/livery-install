Алгоритм работы CLI-автоматизатора

1. Аргументы командной строки
   Программа должна принимать путь к ZIP-файлу как аргумент:
   install-livery.exe "C:\Downloads\lufthansa_777f.zip"
2. Блок самодиагностики (Startup)
   Детекция MSFS: CLI проверяет пути (Steam/Store). Если не находит — запрашивает ввод пути или ищет UserCfg.opt.
   Проверка окружения: Ищет MSFSLayoutGenerator.exe в папке с программой. Если его нет — выводит красную ошибку и завершает работу.
   Проверка базы: Подтверждает наличие папки pmdg-aircraft-77f-liveries в Community.
3. Процесс установки (Интерактивный лог)
   Шаг 1: Распаковка и Анализ
   Создание временной папки %TEMP%\pmdg_livery_extract.
   Распаковка ZIP.
   Поиск данных: Скрипт сканирует папку на наличие livery.json.
   Парсинг JSON: Извлечение значения "atcId".
   Ошибка: Если atcId не найден, CLI просит пользователя ввести ID вручную в консоли.
   Шаг 2: Создание структуры в Community
   Определение имени папки ливреи (берется из имени ZIP-файла или названия папки внутри архива).
   Путь: Community\pmdg-aircraft-77f-liveries\SimObjects\Airplanes\<LIVERY_NAME>.
   Копирование всех файлов из архива в этот путь.
   Шаг 3: Настройка конфигов (Системная часть)
   Поиск файла options.ini в новой папке.
   Переименование: options.ini -> <atcId>.ini.
   Перемещение файла в системную папку PMDG:
   Вывод в консоль: [INFO] Moving .ini to PMDG Work folder...
   Удаление options.ini из папки Community.
   Шаг 4: Обновление Layout (Финализация)
   Запуск процесса: MSFSLayoutGenerator.exe "path\to\layout.json".
   Ожидание завершения (Exit Code 0).
   Вывод в консоль: [SUCCESS] Layout.json updated.
4. Обработка ошибок
   Если файл уже существует — CLI должен спросить: Overwrite? (y/n).
   Использование блоков try-except (Python) или проверок err != nil (Go) на каждом этапе копирования.
   Пример вывода в терминале:
   bash
   $ pmdg-tool install air_france_77f.zip

[>] Detecting MSFS installation... Found (Steam)
[>] Extracting air_france_77f.zip... Done.
[>] Found ATC ID: F-GUOC
[>] Creating directory: pmdg-aircraft-77f-liveries/SimObjects/Airplanes/AF_777F
[>] Moving options.ini to PMDG Work folder as F-GUOC.ini... Done.
[>] Running MSFSLayoutGenerator...
[SUCCESS] Livery "Air France" installed successfully!

---

### 🧭 Список команд (CLI interface)

```bash
pmdg-livery install <path-to-zip>  # основная команда
pmdg-livery detect                 # только проверка окружения (для дебага)
pmdg-livery version                # версия
```

> Предполагаемое имя бинарника: `pmdg-livery`

---

### 📦 Команды и флаги

#### 1. `install <zip-file>`

**Назначение**: Установка ливреи из ZIP-файла.

- **Аргумент**: путь к `.zip` файлу (обязательный)
- **Флаги (опционально)**:
  - `--msfs-path <path>` — явно указать путь к MSFS (если авто-детекция не сработала)
  - `--force` — перезаписывать без подтверждения (`y` по умолчанию)
  - `--dry-run` — показать, что будет сделано, но не выполнять действия

#### 2. `detect`

**Назначение**: Только проверка окружения (MSFS, MSFSLayoutGenerator, Community-папка)

- Без аргументов
- Полезно для отладки

#### 3. `version`

**Назначение**: Вывести версию приложения

---

### 🔧 Алгоритм выполнения команды `install`

> Всё происходит в порядке, близком к твоему описанию, но адаптировано под Go и Cobra.

---

#### 📌 Шаг 0: Валидация входных данных

- Проверить, что аргумент — это существующий ZIP-файл
- Если `--msfs-path` задан — использовать его, иначе искать

---

#### 🔍 Шаг 1: Блок самодиагностики (Startup Checks)

1. **Поиск MSFS**:

   - Проверить стандартные пути:
     - `C:\Program Files\WindowsApps\Microsoft.FlightSimulator_...` (Store)
     - `C:\Program Files (x86)\Steam\steamapps\common\MicrosoftFlightSimulator` (Steam)
   - Альтернатива: прочитать `UserCfg.opt` в `%LOCALAPPDATA%\Packages\Microsoft.FlightSimulator_...\LocalCache`
   - Если не найдено — запросить у пользователя или завершить с ошибкой

2. **Проверка `MSFSLayoutGenerator.exe`**:

   - Искать в той же папке, где запущен CLI (`os.Executable()` → dir)
   - Если отсутствует — `log.Fatal("MSFSLayoutGenerator.exe not found in current directory")`

3. **Проверка Community-папки**:
   - Путь: `<MSFS>/Community/pmdg-aircraft-77f-liveries`
   - Если папки нет — создать с подпапками:
     ```
     pmdg-aircraft-77f-liveries/
       └── SimObjects/
           └── Airplanes/
     ```

---

#### 📂 Шаг 2: Распаковка и анализ ZIP

1. Создать временную папку: `os.MkdirTemp(os.TempDir(), "pmdg_livery_extract")`
2. Распаковать ZIP туда (`archive/zip`)
3. Рекурсивно искать **`livery.json`**
4. Прочитать JSON, найти поле `"atcId"`
   - Если не найден → запросить у пользователя ввод в консоль (`fmt.Scanln`)
5. Определить имя ливреи:
   - По названию папки внутри ZIP (если одна корневая папка)
   - Иначе — по имени ZIP без расширения

---

#### 🗂️ Шаг 3: Копирование в Community

- Целевой путь: `<Community>/pmdg-aircraft-77f-liveries/SimObjects/Airplanes/<LIVERY_NAME>`
- Если папка уже существует:
  - Если `--force` — удалить
  - Иначе — спросить: `Overwrite existing livery? (y/n)`
- Скопировать **всё содержимое** распакованной папки в целевую

---

#### ⚙️ Шаг 4: Обработка `options.ini`

1. Найти `options.ini` в `<LIVERY_NAME>/`
2. Переименовать в `<atcId>.ini`
3. Переместить в **PMDG Work Folder**:
   - Обычно: `%LOCALAPPDATA%\PMDG\PMDG 777X\`
   - Если папки нет — создать
4. Удалить оригинальный `options.ini` из Community (чтобы не мешал)

> Вывод: `[INFO] Moving .ini to PMDG Work folder as F-GUOC.ini...`

---

#### 🧱 Шаг 5: Обновление Layout

1. Путь к `layout.json`: `<MSFS>/Community/layout.json`
2. Запустить:
   ```go
   cmd := exec.Command("MSFSLayoutGenerator.exe", layoutPath)
   err := cmd.Run()
   ```
3. Проверить `ExitCode == 0`
4. Вывести: `[SUCCESS] Layout.json updated.`

---

#### ✅ Финал

- Вывести `[SUCCESS] Livery "Air France" installed successfully!`
- Удалить временную папку (`os.RemoveAll(tempDir)`)

---

### 🛑 Обработка ошибок

- Каждая операция — в отдельной функции с возвратом `error`
- Использовать `log.Fatalf()` только на критических этапах (например, нет MSFSLayoutGenerator)
- На этапах копирования/перемещения — `if err != nil { handleGracefully() }`
- Завершать с кодом `1` при ошибках

---

### 💡 Структура проекта (рекомендовано)

```
/cmd/
  /pmdg-livery/
    main.go
    install.go
    detect.go
    version.go
/internal/
  /msfs/
    detect.go
    paths.go
  /livery/
    extract.go
    parse.go
    install.go
  /layout/
    generator.go
  /utils/
    prompt.go     // для y/n вопросов
    cleanup.go
```

---

### Пример вызова Cobra-команды (install.go)

```go
var installCmd = &cobra.Command{
	Use:   "install [zip-file]",
	Short: "Install PMDG 777F livery from ZIP",
	Args:  cobra.ExactArgs(1),
	RunE: func(cmd *cobra.Command, args []string) error {
		zipPath := args[0]
		msfsPath, _ := cmd.Flags().GetString("msfs-path")
		force, _ := cmd.Flags().GetBool("force")
		dryRun, _ := cmd.Flags().GetBool("dry-run")

		return installLivery(zipPath, msfsPath, force, dryRun)
	},
}

func init() {
	installCmd.Flags().String("msfs-path", "", "Path to MSFS installation")
	installCmd.Flags().BoolP("force", "f", false, "Overwrite without confirmation")
	installCmd.Flags().Bool("dry-run", false, "Simulate installation")
	rootCmd.AddCommand(installCmd)
}
```

---
